# Blank HTML/CSS/JS Project

This is a minimal starter project.

How to use:

- Open `index.html` in your browser.
- Or serve the folder locally with Python:

```bash
python -m http.server --directory "." 8000
```
