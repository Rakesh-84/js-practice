document.addEventListener('DOMContentLoaded',()=>{
  const p = document.getElementById('msg');
  if(p) p.textContent = 'Project ready — edit script.js to add JS.';
  console.log('Blank project loaded');
});
